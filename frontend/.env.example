# Base URL of the uploader HTTP API (server.js).
VITE_BASEURL=http://localhost:4000

# Optional shared token; must match UPLOAD_API_TOKEN on the backend if set.
VITE_UPLOAD_API_TOKEN=

# Basic auth gate (client-side). Change these for your team.
VITE_AUTH_USERNAME=admin
VITE_AUTH_PASSWORD=changeme
